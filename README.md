# Ongea
OngeaLabs is a Streamlit-based Text-to-Speech (TTS) studio for African languages. It generates natural-sounding WAV clips from single text prompts or batch scripts using Meta MMS/community voice models, with speed/pitch controls and an in-session audio library. Runs on CPU compute (no ChatGPT/OpenAI API tokens).
